:root{
  --bg-1: #ff7eb3;
  --bg-2: #7afcff;
  --accent: #2b2d42;
  --muted: #6c757d;
  --card: rgba(255,255,255,0.95);
  --glass: rgba(255,255,255,0.12);
  --success: #2ecc71;
  --danger: #e74c3c;
  --transition: 260ms cubic-bezier(.2,.9,.3,1);
  --radius: 12px;
  --shadow: 0 8px 28px rgba(30,30,60,0.12);
}

*{box-sizing:border-box}
html,body{height:100%;margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial;}
body{
  background: linear-gradient(135deg,var(--bg-1) 0%, #ffd87a 25%, var(--bg-2) 100%);
  -webkit-font-smoothing:antialiased;
  color:var(--accent);
  display:flex;flex-direction:column;min-height:100vh;
}

/* Header */
.site-header{
  display:flex;justify-content:space-between;align-items:center;padding:18px 28px;background:transparent;
}
.brand h1{margin:0;font-size:1.4rem;letter-spacing:0.6px;}
.brand .tag{margin:2px 0 0;font-size:0.85rem;color:rgba(43,45,66,0.8)}
.top-nav{display:flex;gap:10px;align-items:center;}
.nav-btn{
  background:transparent;border:none;padding:8px 12px;border-radius:8px;color:var(--accent);cursor:pointer;font-weight:600;
  transition:all var(--transition);
}
.nav-btn:hover{background:var(--glass);transform:translateY(-2px)}
.nav-btn.primary{background:var(--accent);color:#fff;padding:9px 14px}
.nav-btn.danger{background:transparent;color:var(--danger)}
.user-area{display:flex;gap:8px;align-items:center}
.hidden{display:none}

/* Main layout */
main{flex:1;padding:18px 24px;max-width:1100px;margin:0 auto;width:100%}

/* Hero */
.hero{display:grid;grid-template-columns:1fr 320px;gap:20px;align-items:center;margin-bottom:22px}
.hero-card{
  background:var(--card);padding:26px;border-radius:16px;box-shadow:var(--shadow);
}
.hero-card h2{margin:0 0 8px;font-size:1.6rem}
.hero-card p{margin:0 0 16px;color:var(--muted)}
.hero-actions{display:flex;gap:12px}
.cta{background:var(--accent);color:white;border:none;padding:10px 16px;border-radius:10px;font-weight:700;cursor:pointer}
.cta.outline{background:transparent;border:2px solid rgba(43,45,66,0.08);color:var(--accent)}

/* Clusters */
.section-header{margin-bottom:12px}
.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:14px}
.card{
  background:rgba(255,255,255,0.95);padding:16px;border-radius:14px;box-shadow:0 6px 20px rgba(16,24,40,0.06);
  display:flex;flex-direction:column;justify-content:space-between;transition:transform var(--transition);
}
.card:hover{transform:translateY(-6px)}
.card h3{margin:0 0 6px}
.card p{margin:0 0 12px;color:var(--muted);font-size:0.95rem}
.card-actions{display:flex;gap:8px}
.btn-sm{padding:8px 10px;border-radius:8px;border:none;cursor:pointer;background:rgba(43,45,66,0.06);font-weight:600}
.btn-sm.primary{background:var(--accent);color:#fff}
.save-cluster{background:linear-gradient(90deg,#ffd87a,#ff7eb3);color:#1b1b1b}

/* Quiz */
.quiz-card{background:var(--card);padding:16px;border-radius:12px;box-shadow:var(--shadow)}
.quiz-card form{display:flex;flex-direction:column;gap:10px}
.quiz-card label{font-weight:600}
.quiz-result{margin-top:12px;padding:12px;border-radius:10px;background:linear-gradient(90deg,#ffffff,#fff8f1);box-shadow:inset 0 1px 0 rgba(255,255,255,0.5)}

/* Dashboard */
.dashboard{margin-top:18px}
.saved-list{background:var(--card);padding:14px;border-radius:12px;box-shadow:var(--shadow)}

/* Footer */
footer{text-align:center;padding:12px 0;color:rgba(27,29,66,0.7)}

/* Modal / Auth */
.modal{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:linear-gradient(0deg,rgba(20,20,30,0.35),rgba(20,20,30,0.35));backdrop-filter:blur(6px);z-index:60}
.modal-content{width:100%;max-width:520px;background:rgba(255,255,255,0.98);padding:18px;border-radius:14px;position:relative;box-shadow:0 20px 60px rgba(10,10,30,0.25)}
.modal-close{position:absolute;right:12px;top:12px;border:none;background:transparent;font-size:18px;cursor:pointer}
.tabs{display:flex;gap:8px;margin-bottom:12px}
.tab{flex:1;padding:10px;border-radius:10px;border:none;background:rgba(0,0,0,0.04);cursor:pointer}
.tab.active{background:var(--accent);color:#fff}
.tab-panel{display:block}
.tab-panel.hidden{display:none}
form input, form select{width:100%;padding:10px;border-radius:8px;border:1px solid rgba(30,30,40,0.06)}
.form-actions{margin-top:10px;display:flex;gap:8px}
.auth-feedback{margin-top:10px;color:var(--danger);font-weight:600}

/* Toast */
.toast{position:fixed;right:18px;bottom:18px;background:#111827;color:white;padding:12px 14px;border-radius:10px;box-shadow:0 10px 30px rgba(0,0,0,0.3);z-index:80}
.toast.hidden{display:none}

/* Responsiveness */
@media (max-width:900px){
  .hero{grid-template-columns:1fr;gap:12px}
  .top-nav{gap:6px}
  .nav-btn.primary{display:none}
}
